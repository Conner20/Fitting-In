"use client";
import { useEffect, useState } from "react";
import { RefreshCw, ShieldCheck, Trash2 } from "lucide-react";
import { Input } from "@/components/ui/input";
import { PasswordInput } from "@/components/ui/password-input";

type User={id:string;username:string|null;name:string|null;email:string|null;role:string|null;hasAdminAccess:boolean;isConfiguredAdmin:boolean;lastActiveAt:string|null;gymAccessCount:number};
export default function AdminUserManager(){const[users,setUsers]=useState<User[]>([]),[query,setQuery]=useState(""),[loading,setLoading]=useState(true),[canManage,setCanManage]=useState(false),[selected,setSelected]=useState<string[]>([]),[password,setPassword]=useState(""),[message,setMessage]=useState("");const load=async()=>{setLoading(true);const r=await fetch(`/api/admin/users?q=${encodeURIComponent(query)}`,{cache:"no-store"});const data=await r.json().catch(()=>({}));if(r.ok){setUsers(data.users??[]);setCanManage(Boolean(data.canManageUsers))}else setMessage(data.error??"Unable to load users.");setLoading(false)};useEffect(()=>{const id=setTimeout(()=>void load(),200);return()=>clearTimeout(id)},[query]);const remove=async()=>{if(!selected.length||!password)return setMessage("Enter your password and select at least one account.");if(!confirm(`Delete ${selected.length} selected account${selected.length===1?"":"s"}? This cannot be undone.`))return;for(const targetUserId of selected){const r=await fetch("/api/user/delete",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({password,targetUserId})});const data=await r.json().catch(()=>({}));if(!r.ok){setMessage(data.message??"Unable to delete account.");return}}setSelected([]);setPassword("");setMessage("Selected accounts deleted.");await load()};return <div className="space-y-5"><div className="flex flex-wrap gap-3"><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search name, username, or email" className="max-w-md"/><button onClick={()=>void load()} className="rounded-xl border px-4 py-2 transition hover:border-[#22c55e] hover:text-[#22c55e]"><RefreshCw className={`h-4 w-4 ${loading?"animate-spin":""}`}/></button></div>{message&&<p className="rounded-xl border border-black/10 bg-white p-3 text-sm dark:border-white/10 dark:bg-white/5">{message}</p>}<div className="overflow-x-auto rounded-2xl border border-black/10 bg-white dark:border-white/10 dark:bg-white/5"><table className="w-full min-w-[760px] text-sm"><thead><tr className="text-left text-xs uppercase tracking-wide text-zinc-500"><th className="p-4">Select</th><th>Account</th><th>Role</th><th>Gym access</th><th>Admin</th><th>Last active</th></tr></thead><tbody>{users.map(user=><tr key={user.id} className="border-t border-black/5 dark:border-white/10"><td className="p-4"><input type="checkbox" disabled={user.hasAdminAccess} checked={selected.includes(user.id)} onChange={()=>setSelected(a=>a.includes(user.id)?a.filter(id=>id!==user.id):[...a,user.id])} className="h-4 w-4 accent-[#22c55e]"/></td><td><b>{user.name||user.username||"Unnamed user"}</b><p className="text-xs text-zinc-500">{user.email}</p></td><td>{user.role||"—"}</td><td>{user.gymAccessCount}</td><td>{user.hasAdminAccess?<span className="inline-flex items-center gap-1 text-[#22c55e]"><ShieldCheck className="h-4 w-4"/>Yes</span>:"No"}</td><td>{user.lastActiveAt?new Date(user.lastActiveAt).toLocaleString():"No activity"}</td></tr>)}</tbody></table>{!loading&&!users.length&&<p className="p-8 text-center text-zinc-500">No accounts found.</p>}</div>{canManage&&<div className="flex flex-wrap items-end gap-3 rounded-2xl border border-red-200 bg-red-50 p-4 dark:border-red-900 dark:bg-red-950/20"><label className="min-w-[260px] flex-1 text-sm font-medium">Admin password<PasswordInput value={password} onChange={e=>setPassword(e.target.value)} className="mt-1"/></label><button onClick={()=>void remove()} disabled={!selected.length} className="flex items-center gap-2 rounded-xl bg-red-600 px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-red-700 disabled:opacity-40"><Trash2 className="h-4 w-4"/>Delete selected</button></div>}</div>}

import { getServerSession } from "next-auth";import{redirect}from"next/navigation";import AdminUserManager from"@/components/AdminUserManager";import AdminNav from"@/components/AdminNav";import MobileHeader from"@/components/MobileHeader";import{authOptions}from"@/lib/auth";import{hasAdminAccessByEmail}from"@/lib/admin";
export default async function AdminPage(){const session=await getServerSession(authOptions);if(!session?.user?.email||!(await hasAdminAccessByEmail(session.user.email)))redirect("/");return <main className="min-h-screen bg-[#f8f8f8] text-black dark:bg-[#050505] dark:text-white"><MobileHeader title="admin console" href="/admin"/><header className="hidden border-b border-black/5 bg-white px-10 py-6 dark:border-white/10 dark:bg-[#050505] lg:block"><p className="text-sm uppercase tracking-wide text-zinc-500">Admin console</p><h1 className="text-3xl font-bold">Manage users</h1><div className="mt-4"><AdminNav active="users"/></div></header><section className="mx-auto max-w-6xl space-y-6 px-4 py-10"><div className="lg:hidden"><AdminNav active="users" mobile/></div><p className="text-sm text-zinc-600 dark:text-white/60">Search and manage authentication accounts and gym access.</p><AdminUserManager/></section></main>}

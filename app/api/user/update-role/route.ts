import{getServerSession}from"next-auth";import{encode}from"next-auth/jwt";import{NextResponse}from"next/server";import{authOptions}from"@/lib/auth";import{db}from"@/prisma/client";
export async function POST(request:Request){const session=await getServerSession(authOptions);if(!session?.user?.email)return NextResponse.json({message:"Unauthorized"},{status:401});const body=await request.json().catch(()=>({}));if(body.role==="GYM")return NextResponse.json({message:"Gym roles are assigned only after a gym listing is verified."},{status:403});const role="TRAINEE";const location=typeof body.location==="string"?body.location.trim():undefined;const user=await db.user.update({where:{email:session.user.email.toLowerCase()},data:{role,...(location?{location}:{})},select:{id:true,email:true,role:true}});const maxAge=authOptions.session?.maxAge??60*60*24*365*100,token=await encode({secret:process.env.NEXTAUTH_SECRET!,maxAge,token:{sub:user.id,email:user.email,role:user.role}});const response=NextResponse.json({user,message:"Account updated"});response.cookies.set(process.env.NODE_ENV==="production"?"__Secure-next-auth.session-token":"next-auth.session-token",token,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge});response.cookies.set("next-auth.callback-url","/",{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge});return response}
